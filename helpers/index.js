const setup = require('./setup');
const render = require('./render');
const transform = require('./transform');
const pause = require('./pause');
const progress = require('./progress');

module.exports = {setup, render, transform, pause, progress};

